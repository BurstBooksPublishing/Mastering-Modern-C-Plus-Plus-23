#include <algorithm>
#include <vector>

struct Person {
    int age;
};

void sortPeopleByAge(std::vector<Person>& people) {
    std::sort(people.begin(), people.end(),
              [](const Person& a, const Person& b) { return a.age < b.age; });
}