#include <type_traits>

template <typename T>
constexpr T factorial(T n) noexcept {
    static_assert(std::is_integral_v<T>, "Integral type required");
    return n <= 1 ? 1 : n * factorial(n - 1);
}

constexpr unsigned int result = factorial(5u);   // compile-time value 120