#include <fstream>
#include <iostream>

int main() {
    std::ifstream inFile("nonexistent_file.txt");
    if (!inFile) {
        std::cerr << "Error: cannot open file 'nonexistent_file.txt'\n";
        return 1;
    }
    // Process file contents here
    return 0;
}