#include <algorithm>
#include <iostream>
#include <vector>

int main() {
    std::vector<int> v{3, 1, 4, 1, 5, 9, 2, 6};
    std::ranges::sort(v);               // ascending order
    for (int x : v) std::cout << x << ' ';
    std::cout << '\n';
    return 0;
}