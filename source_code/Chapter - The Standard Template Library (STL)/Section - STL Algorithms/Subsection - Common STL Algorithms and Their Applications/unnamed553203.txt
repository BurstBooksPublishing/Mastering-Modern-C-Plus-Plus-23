#include <vector>
#include <algorithm>
#include <iostream>

int main() {
    std::vector<int> v{1, 2, 3, 2, 4};

    // Replace every occurrence of 2 with 99
    std::replace(v.begin(), v.end(), 2, 99);

    // Output: 1 99 3 99 4
    for (const int val : v) std::cout << val << ' ';
}