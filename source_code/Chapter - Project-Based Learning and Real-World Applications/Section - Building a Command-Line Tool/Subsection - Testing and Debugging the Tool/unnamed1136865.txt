#include <spdlog/spdlog.h>
#include <memory>
#include <stdexcept>

int main() {
    spdlog::set_level(spdlog::level::info);
    spdlog::info("Starting the tool...");

    try {
        // TODO: Replace with actual tool logic
    } catch (const std::exception& e) {
        spdlog::error("Exception occurred: {}", e.what());
        return EXIT_FAILURE;
    }

    spdlog::info("Tool finished.");
    return EXIT_SUCCESS;
}