#include <mutex>
#include <atomic>

namespace {
    std::mutex mtx;
    std::atomic<int> shared_data{0};
}

void safeIncrement() noexcept {
    std::lock_guard<std::mutex> lock(mtx);
    ++shared_data;
}