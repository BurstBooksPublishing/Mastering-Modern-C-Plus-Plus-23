#include <iterator>
#include <type_traits>

template <typename T>
void process(const T& container) {
    static_assert(std::is_same_v<typename std::iterator_traits<decltype(std::begin(container))>::iterator_category,
                                 std::random_access_iterator_tag>,
                  "Container must provide random-access iterators");

    auto it = std::begin(container);
    if (it != std::end(container)) {
        // Critical processing logic
    }
}