#include <iostream>

int main() {
    const int x = 10;               // constant value for safety
    std::cout << (x > 5 ? "x is greater than 5\n"
                        : "x is not greater than 5\n");
    return 0;
}