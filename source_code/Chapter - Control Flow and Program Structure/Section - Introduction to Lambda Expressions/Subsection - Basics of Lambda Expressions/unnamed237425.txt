#include <stdexcept>

constexpr int square(int x) noexcept {
    return x * x;
}

int main() {
    static_assert(square(5) == 25, "square(5) must be 25");
    const int result = square(5); // 25
    return 0;
}