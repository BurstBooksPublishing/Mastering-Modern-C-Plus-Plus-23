#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec{1, 2, 3, 4, 5};
    vec.push_back(6);

    for (const int& v : vec) {
        std::cout << v << ' ';
    }
    std::cout << '\n';
    return 0;
}