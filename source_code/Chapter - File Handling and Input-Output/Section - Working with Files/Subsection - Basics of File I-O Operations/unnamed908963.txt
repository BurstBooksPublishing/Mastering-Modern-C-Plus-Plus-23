#include <fstream>
#include <iostream>

int main() {
    std::ofstream outFile("example.txt", std::ios::out);
    if (!outFile) {
        std::cerr << "Failed to open file.\n";
        return 1;
    }
    outFile << "Hello, File I/O!\n";
    return 0;
}