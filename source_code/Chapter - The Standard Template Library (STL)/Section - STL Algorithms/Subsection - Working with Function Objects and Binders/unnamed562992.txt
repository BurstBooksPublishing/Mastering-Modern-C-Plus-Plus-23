#include <algorithm>
#include <vector>
#include <iostream>

void print_sum(int a, int b) {
    std::cout << (a + b) << '\n';
}

int main() {
    std::vector<int> v{1, 2, 3, 4, 5};
    std::for_each(v.begin(), v.end(), [](int x) { print_sum(x, 5); });
}