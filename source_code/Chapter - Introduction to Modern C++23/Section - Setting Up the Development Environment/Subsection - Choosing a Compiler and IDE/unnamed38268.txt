#include <iostream>
#include <string>
#include <stdexcept>
#include <cstdlib>

int main() {
    try {
        const std::string cmd = "g++ --version";
        const int ret = std::system(cmd.c_str());
        if (ret != 0) {
            throw std::runtime_error("Command failed with exit code " + std::to_string(ret));
        }
    } catch (const std::exception& ex) {
        std::cerr << "Error: " << ex.what() << '\n';
        return EXIT_FAILURE;
    }
    return EXIT_SUCCESS;
}