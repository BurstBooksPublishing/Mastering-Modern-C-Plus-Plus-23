#include <vector>
#include <iostream>

int main() {
    const std::vector<int> vec{1, 2, 3};

    for (const int value : vec) {
        std::cout << value << ' ';
    }
    std::cout << '\n';
}