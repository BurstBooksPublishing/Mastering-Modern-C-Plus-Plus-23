#include <algorithm>
#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec{3, 1, 4, 1, 5, 9, 2, 6};

    std::sort(vec.begin(), vec.end());

    for (const int value : vec) {
        std::cout << value << ' ';
    }
    std::cout << '\n';
    return 0;
}