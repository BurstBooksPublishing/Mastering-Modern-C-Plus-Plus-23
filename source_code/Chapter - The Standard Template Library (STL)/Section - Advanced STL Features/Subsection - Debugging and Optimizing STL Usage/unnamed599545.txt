#include <algorithm>
#include <vector>

std::vector<int> vec{1, 2, 3, 4, 5};

// Remove even numbers in-place
vec.erase(std::remove_if(vec.begin(), vec.end(),
                         [](int x) { return x % 2 == 0; }),
          vec.end());