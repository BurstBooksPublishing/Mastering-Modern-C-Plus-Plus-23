#include <vector>
#include <algorithm>
#include <iterator>

std::vector<int> vec{1, 2, 3};
std::vector<int> result;
result.reserve(vec.size());          // avoid reallocations
std::copy(vec.begin(), vec.end(), std::back_inserter(result));