#include <iostream>

int main() {
    int x{};
    std::cin >> x;

    if (x > 10) {
        std::cout << "greater\n";
    } else if (x == 10) {
        std::cout << "equal\n";
    } else {
        std::cout << "less\n";
    }

    return 0;
}