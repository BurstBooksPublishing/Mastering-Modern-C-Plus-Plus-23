#include <algorithm>
#include <iterator>
#include <type_traits>

template <class Container, class Func>
void apply_to_each(Container& container, Func&& func)
{
    using std::begin;
    using std::end;

    std::transform(begin(container), end(container),
                   begin(container),
                   std::forward<Func>(func));
}