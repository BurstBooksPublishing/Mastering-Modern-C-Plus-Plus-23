#include <functional>
#include <stdexcept>

const std::function<int(int)> factorial = [](int n) -> int {
    if (n < 0) throw std::invalid_argument("Factorial is undefined for negative numbers.");
    return (n <= 1) ? 1 : n * factorial(n - 1);
};