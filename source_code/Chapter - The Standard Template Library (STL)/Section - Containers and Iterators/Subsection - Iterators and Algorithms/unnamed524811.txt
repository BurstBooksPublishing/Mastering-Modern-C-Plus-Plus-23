#include <algorithm>
#include <iostream>
#include <vector>

int main() {
    std::vector<int> vec{5, 3, 4, 1, 2};
    std::sort(vec.begin(), vec.end());

    for (const int value : vec) {
        std::cout << value << ' ';
    }
    std::cout << '\n';
    return 0;
}